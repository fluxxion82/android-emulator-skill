{
  "profile": "gradle-8.13",
  "recorded_at": "2026-09-03T08:07:08Z",
  "gradle_version": "8.13",
  "scaffolds": [
    "tests/fixtures/scaffold/flavored",
    "tests/fixtures/scaffold/compile",
    "tests/fixtures/scaffold/where-block"
  ],
  "note": "Durations and absolute paths are normalised so a re-record diffs only on real format changes. The JUnit XML additionally has its hostname, timestamp and per-test time normalised: Gradle writes the recording machine's hostname into every testsuite element.",
  "fixtures": [
    {
      "name": "gradle_task_not_found",
      "file": "gradle_task_not_found.txt",
      "command": "gradle --console=plain :app:assembleNoSuchVariantAtAll",
      "scaffold": "tests/fixtures/scaffold/flavored",
      "produced_artifact": null,
      "description": "A task that genuinely does not exist. Produces Gradle's 'FAILURE: Build failed with an exception.' / '* What went wrong:' block. Nothing in gradle/results.py parses this, so the skill reports 'Build: FAILED (0 errors, 0 warnings)' with no diagnostic.",
      "catches": [
        "A6"
      ],
      "bytes": 745
    },
    {
      "name": "gradle_task_ambiguous",
      "file": "gradle_task_ambiguous.txt",
      "command": "gradle --console=plain :app:assembleProstaging",
      "scaffold": "tests/fixtures/scaffold/flavored",
      "produced_artifact": null,
      "description": "An abbreviated task name matching more than one task. Gradle lists the candidates in the '* What went wrong:' block \u2014 exactly the detail an agent needs and currently never sees.",
      "catches": [
        "A6"
      ],
      "bytes": 804
    },
    {
      "name": "gradle_miscased_variant_resolves",
      "file": "gradle_miscased_variant_resolves.txt",
      "command": "gradle --console=plain :app:assembleProstagingdebug",
      "scaffold": "tests/fixtures/scaffold/flavored",
      "produced_artifact": null,
      "description": "Evidence that S2 is NOT build-breaking. str.capitalize() turns 'proStagingDebug' into 'Prostagingdebug', but Gradle's name abbreviation matching is case-insensitive and resolves it to assembleProStagingDebug. BUILD SUCCESSFUL, no deprecation warning. The generated name is still wrong and should be fixed, but it does not fail the build.",
      "catches": [
        "S2"
      ],
      "bytes": 117
    },
    {
      "name": "gradle_build_successful",
      "file": "gradle_build_successful.txt",
      "command": "gradle --console=plain :app:assembleProStagingDebug",
      "scaffold": "tests/fixtures/scaffold/flavored",
      "produced_artifact": null,
      "description": "Baseline success output, for asserting the parser does not cry wolf.",
      "catches": [],
      "bytes": 117
    },
    {
      "name": "gradle_kotlin_compile_error",
      "file": "gradle_kotlin_compile_error.txt",
      "command": "gradle --console=plain --rerun-tasks :kotlin-error:compileKotlin",
      "scaffold": "tests/fixtures/scaffold/compile",
      "produced_artifact": null,
      "description": "A real Kotlin compile error, and the reason this fixture had to exist. K2 prints `e: file:///abs/path/Broken.kt:5:13 Unresolved reference 'x'.` \u2014 a file:// URI, colon-separated line:column, no parentheses, no colon before the message, and a trailing full stop. results.py's _split_kotlin_body matches Kotlin 1.x's `e: /path.kt: (5, 13): unresolved reference: x` instead, so on any Kotlin 2.x build the error is reported with file, line and column all None. Note too that the `> Task ... FAILED` marker and the `e:` line both appear, so one mistake yields two errors.",
      "catches": [],
      "bytes": 1339
    },
    {
      "name": "gradle_kotlin_compile_warning",
      "file": "gradle_kotlin_compile_warning.txt",
      "command": "gradle --console=plain --rerun-tasks :kotlin-warning:compileKotlin",
      "scaffold": "tests/fixtures/scaffold/compile",
      "produced_artifact": null,
      "description": "A real Kotlin warning, in the same K2 shape as the error: `w: file:///abs/path.kt:7:21 'fun retired(): Int' is deprecated. \u2026`. Recorded separately from the error because a compiler that has errored stops reporting warnings \u2014 measured: a module holding both prints only the error \u2014 so 'errors and warnings in one log' is not a shape either compiler produces from one module. This capture also carries the Kotlin Gradle plugin's own multi-line `w:` block, whose body is prose rather than a location and whose continuation lines carry no `w:` prefix at all.",
      "catches": [],
      "bytes": 880
    },
    {
      "name": "gradle_javac_compile_error",
      "file": "gradle_javac_compile_error.txt",
      "command": "gradle --console=plain --rerun-tasks :java-error:compileJava",
      "scaffold": "tests/fixtures/scaffold/compile",
      "produced_artifact": null,
      "description": "A real javac error. The `path.java:LINE: error: message` shape is the one thing the hand-written sample got right, but it omits everything around it: the source line and caret, the indented `symbol:`/`location:` continuation lines, a trailing `N errors` count, one mistake reported TWICE by javac itself, and then the whole block repeated a third and fourth time \u2014 indented by two spaces \u2014 inside Gradle's `* What went wrong:` section. _JAVAC_ERROR_RE's `[^:]+` file group matches leading spaces, so it re-matches the indented copies and one missing type is counted four times.",
      "catches": [],
      "bytes": 1836
    },
    {
      "name": "gradle_javac_compile_warning",
      "file": "gradle_javac_compile_warning.txt",
      "command": "gradle --console=plain --rerun-tasks :java-warning:compileJava",
      "scaffold": "tests/fixtures/scaffold/compile",
      "produced_artifact": null,
      "description": "A real javac warning: `path.java:6: warning: [deprecation] retired() in Retired has been deprecated`, followed by the source line, a caret and `1 warning`. The `[deprecation]` lint-category tag rides inside the message, which no hand-written sample had. Requires `-Xlint:deprecation` and a caller in a different class \u2014 javac says nothing when a class deprecates and calls its own method.",
      "catches": [],
      "bytes": 858
    },
    {
      "name": "gradle_junit_error_ignorefailures",
      "file": "gradle_junit_error_ignorefailures.txt",
      "command": "gradle --console=plain --rerun-tasks :junit-error:test",
      "scaffold": "tests/fixtures/scaffold/compile",
      "produced_artifact": null,
      "description": "A test run with three failing tests, one skipped and one passing, under `test { ignoreFailures = true }`. The log says '5 tests completed, 3 failed, 1 skipped' and then **BUILD SUCCESSFUL**, and gradle exits 0. That pairing is the fixture's point: a caller that reads the exit status, or greps for 'BUILD FAILED', concludes the suite passed. Only the JUnit XML (junit_xml_error_case.xml, recorded from the same run) carries the truth.",
      "catches": [
        "X7",
        "D2"
      ],
      "bytes": 1494
    },
    {
      "name": "junit_xml_error_case",
      "file": "junit_xml_error_case.xml",
      "command": "gradle --console=plain --rerun-tasks :junit-error:test",
      "scaffold": "tests/fixtures/scaffold/compile",
      "produced_artifact": "junit-error/build/test-results/test/TEST-com.example.BeforeThrowsTest.xml",
      "description": "The JUnit XML for a class whose @Before throws \u2014 the real thing, which no test in this repo had. Measured shape, and it contradicts the obvious guess: the suite carries failures=\"2\" errors=\"0\" and each testcase holds a <failure type=\"java.lang.IllegalStateException\"> element. There is no <error> element, even though the exception came from setup rather than from an assertion, so a parser keying on <error> to find setup failures finds none. Both testcases are still listed by name although neither body ran, and each carries the same stack trace rooted at setUp. hostname/timestamp/time are normalised by the recorder (see _scrub): Gradle writes the recording machine's hostname into every testsuite element.",
      "catches": [
        "X7",
        "D2"
      ],
      "bytes": 8302
    },
    {
      "name": "junit_xml_passing_case",
      "file": "junit_xml_passing_case.xml",
      "command": "gradle --console=plain --rerun-tasks :junit-error:test",
      "scaffold": "tests/fixtures/scaffold/compile",
      "produced_artifact": "junit-error/build/test-results/test/TEST-com.example.PassingTest.xml",
      "description": "The green half of the same run: tests=\"1\" skipped=\"0\" failures=\"0\" errors=\"0\" and a bare self-closing <testcase>. Recorded because a parser that reports everything as failed and a parser that reports nothing at all look identical against a corpus in which everything failed. Also the second document that aggregate_test_results merges.",
      "catches": [
        "X7",
        "D2"
      ],
      "bytes": 378
    },
    {
      "name": "junit_xml_skipped_case",
      "file": "junit_xml_skipped_case.xml",
      "command": "gradle --console=plain --rerun-tasks :junit-error:test",
      "scaffold": "tests/fixtures/scaffold/compile",
      "produced_artifact": "junit-error/build/test-results/test/TEST-com.example.SkippedAndErrorTest.xml",
      "description": "A real <skipped/> child, from an @Ignore'd test -- the only way a Gradle report gets one, and parse_junit_xml counts them. The suite reads tests=\"2\" skipped=\"1\" failures=\"1\" errors=\"0\". The second method exists to answer the obvious question about the <error> element and settles it: it throws an UnsupportedOperationException, which is NOT an AssertionError, and Gradle still writes <failure type=\"...\">. Taken with junit_xml_error_case (an exception from @Before, also <failure>), no route tried here makes Gradle's writer emit <error> at all.",
      "catches": [
        "X7",
        "D2"
      ],
      "bytes": 4425
    },
    {
      "name": "gradle_where_block",
      "file": "gradle_where_block.txt",
      "command": "gradle --console=plain help",
      "scaffold": "tests/fixtures/scaffold/where-block",
      "produced_artifact": null,
      "description": "A configuration-time failure, which is the only Gradle failure shape that reports a LOCATION. `implementaion` (a typo for `implementation`) makes Gradle emit a `* Where:` section naming the build file and the line number, above the usual `* What went wrong:` block. Every other recorded failure here has no `* Where:` at all, so a parser that only knows the `* What went wrong:` shape throws away the file and line on precisely the failures that have one.",
      "catches": [
        "D8"
      ],
      "bytes": 581
    }
  ]
}
