{
  "profile": "gradle-8.13",
  "recorded_at": "2026-08-31T22:13:58Z",
  "gradle_version": "8.13",
  "scaffold": "tests/fixtures/scaffold/flavored",
  "note": "Durations and absolute paths are normalised so a re-record diffs only on real format changes.",
  "fixtures": [
    {
      "name": "gradle_task_not_found",
      "file": "gradle_task_not_found.txt",
      "command": "gradle --console=plain :app:assembleNoSuchVariantAtAll",
      "description": "A task that genuinely does not exist. Produces Gradle's 'FAILURE: Build failed with an exception.' / '* What went wrong:' block. Nothing in gradle/results.py parses this, so the skill reports 'Build: FAILED (0 errors, 0 warnings)' with no diagnostic.",
      "catches": [
        "A6"
      ],
      "bytes": 745
    },
    {
      "name": "gradle_task_ambiguous",
      "file": "gradle_task_ambiguous.txt",
      "command": "gradle --console=plain :app:assembleProstaging",
      "description": "An abbreviated task name matching more than one task. Gradle lists the candidates in the '* What went wrong:' block \u2014 exactly the detail an agent needs and currently never sees.",
      "catches": [
        "A6"
      ],
      "bytes": 804
    },
    {
      "name": "gradle_miscased_variant_resolves",
      "file": "gradle_miscased_variant_resolves.txt",
      "command": "gradle --console=plain :app:assembleProstagingdebug",
      "description": "Evidence that S2 is NOT build-breaking. str.capitalize() turns 'proStagingDebug' into 'Prostagingdebug', but Gradle's name abbreviation matching is case-insensitive and resolves it to assembleProStagingDebug. BUILD SUCCESSFUL, no deprecation warning. The generated name is still wrong and should be fixed, but it does not fail the build.",
      "catches": [
        "S2"
      ],
      "bytes": 117
    },
    {
      "name": "gradle_build_successful",
      "file": "gradle_build_successful.txt",
      "command": "gradle --console=plain :app:assembleProStagingDebug",
      "description": "Baseline success output, for asserting the parser does not cry wolf.",
      "catches": [],
      "bytes": 117
    }
  ]
}
